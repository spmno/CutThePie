<%= form_for(@work_record) do |f| %>
  <% if @work_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_record.errors.count, "error") %> prohibited this work_record from being saved:</h2>

      <ul>
      <% @work_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.select :user_id, user_id_options %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :record_date %><br>
    <%= f.date_select :record_date %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.text_field :duration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
